import { MutationTree } from 'vuex'
import { ExampleStateInterface } from './state'

const mutation: MutationTree<ExampleStateInterface> = {
  u (/* state: ExampleStateInterface */) {
    // your code
  }
}

export default mutation
